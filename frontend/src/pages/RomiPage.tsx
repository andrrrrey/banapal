import { PageStub } from "@/components/PageStub";
import { RomiIcon } from "@/layout/icons";

export default function RomiPage() {
  return (
    <PageStub
      Icon={RomiIcon}
      title="ROMI и рекомендации"
      description="ROMI по каналам, эффективность кампаний, рекомендации AI по бюджету и рабочий список минус-слов с выгрузкой в формате Яндекс Директа."
      stage="Наполнение — Этап D"
    />
  );
}
