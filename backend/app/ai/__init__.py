"""AI-слой: интерпретация и рекомендации через облачный LLM.

Модели применяются только на слое интерпретации и рекомендаций; расчёт метрик,
контроль сроков и атрибуция выполняются детерминированной логикой без LLM
(раздел 3.6 ТЗ). Вызовы модели выполняются по расписанию (worker), а не на каждое
событие. В mock-режиме инсайты берутся из демо-данных прототипа.
"""
